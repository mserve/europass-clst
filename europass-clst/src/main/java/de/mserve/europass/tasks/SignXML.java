package de.mserve.europass.tasks;

import picocli.CommandLine.Command;

@Command(name = "sign", header = "Signs a single XML file or all XML files in a directory.")
public class SignXML implements Runnable {
    @Override
    public void run() {
    }
}