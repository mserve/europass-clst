package de.mserve.europass.tasks;

import picocli.CommandLine.Command;

@Command(name = "validate", header = "Validates a single XML file or all XML files in a directory.")
public class ValidateXML implements Runnable {
    @Override
    public void run() {
    }
}