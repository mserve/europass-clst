package de.mserve.europass.tasks;

import picocli.CommandLine.Command;

@Command(name = "verify", header = "Verifies a single XML file or all XML files in a directory.")
public class VerifyXML implements Runnable {
    @Override
    public void run() {
    }
}